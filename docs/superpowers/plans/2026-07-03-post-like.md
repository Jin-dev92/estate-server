# 게시글 좋아요 시스템 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 건물 게시글에 좋아요/취소(멱등) 기능을 추가하고, 상세·목록에 `likeCount`·`likedByMe`를 노출하며, 좋아요 시 작성자에게 알림을 보낸다.

**Architecture:** 기존 `board` 헥사고날 구조에 `PostLike` 엔티티·포트·Prisma 리포지토리를 추가한다. 좋아요 정보는 공유 캐시에 넣지 않고 조회 use-case가 라이브로 병합한다. 좋아요 쓰기는 `TransactionRunner`로 insert+outbox를 한 트랜잭션에 묶고, `CommentCreated`를 미러링해 `LikeCreated` → 알림으로 팬아웃한다.

**Tech Stack:** NestJS, Prisma(PostgreSQL), Jest(Prisma 목킹 단위테스트), 기존 Outbox/Kafka/Notification 인프라.

**참조 스펙:** `docs/superpowers/specs/2026-07-03-post-like-design.md`

## Global Constraints

- 매직 스트링 금지 → `const enum`/상수 참조. 이벤트·알림 타입은 반드시 enum에 등록(`EventType`, `NotificationType`).
- 신규/변경 엔드포인트는 Swagger 데코레이터 필수(`@ApiOperation` + 성공 `@ApiResponse` + 4xx `@ApiResponse({ type: ErrorResponseDto })` + `@ApiParam`).
- API 변경 시 README API 표 갱신.
- 테스트: 대상 파일과 동일 디렉토리 `*.spec.ts`, `describe→it` 구조, AAA 패턴, `as any` 금지(`as unknown as PrismaService` 허용), 매직값은 상수화.
- 커밋 메시지: `[M2]{type}: {한글 설명}` 형식.
- 좋아요 수 집계는 **라이브 `COUNT(*)`** — 비정규화 카운터 컬럼 두지 않음.
- 좋아요 정보(`likeCount`/`likedByMe`)는 **공유 캐시(`PostDetail`/`PostSummary`)에 저장하지 않음** — 조회 use-case가 병합.

---

### Task 1: PostLike 스키마 + 마이그레이션

**Files:**
- Modify: `prisma/schema.prisma` (User 모델, Post 모델, 파일 끝에 PostLike 모델 추가)

**Interfaces:**
- Produces: Prisma Client `postLike` 델리게이트(`createMany`/`deleteMany`/`count`/`groupBy`/`findMany`/`findUnique`), 복합 유니크 입력 키 `postId_userId`.

- [ ] **Step 1: `User` 모델에 역참조 관계 추가**

`prisma/schema.prisma`의 `model User { ... }` 안, `comments  Comment[]` 아래 줄에 추가:

```prisma
  postLikes PostLike[]
```

- [ ] **Step 2: `Post` 모델에 역참조 관계 추가**

`model Post { ... }` 안, `comments Comment[]` 아래 줄에 추가:

```prisma
  likes    PostLike[]
```

- [ ] **Step 3: 파일 끝에 `PostLike` 모델 추가**

`prisma/schema.prisma` 맨 아래에 추가:

```prisma
model PostLike {
  id        String   @id @default(cuid())
  postId    String
  post      Post     @relation(fields: [postId], references: [id])
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  createdAt DateTime @default(now())

  // 한 사용자당 한 게시글에 한 번만(멱등·동시성의 근거).
  // 이 복합 인덱스의 선두가 postId → leftmost-prefix로 COUNT(postId) 조회까지 커버.
  // 별도 @@index([postId])는 중복이라 두지 않는다.
  @@unique([postId, userId])
}
```

- [ ] **Step 4: 마이그레이션 생성·적용**

로컬 DB가 떠 있어야 한다(`docker compose up -d`).

Run: `npx prisma migrate dev --name add_post_like`
Expected: `Applying migration ...add_post_like`, `✔ Generated Prisma Client` 출력. `prisma/migrations/<timestamp>_add_post_like/migration.sql` 생성됨.

- [ ] **Step 5: 타입 생성 확인(빌드)**

Run: `npm run build`
Expected: 에러 없이 완료(Prisma Client에 `postLike`가 생겨 이후 타입 참조 가능).

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "[M2]feat: PostLike 스키마·마이그레이션 추가"
```

---

### Task 2: PostLikeRepository 포트 + Prisma 구현

**Files:**
- Create: `src/board/domain/post-like.repository.ts`
- Create: `src/board/infrastructure/prisma-post-like.repository.ts`
- Test: `src/board/infrastructure/prisma-post-like.repository.spec.ts`

**Interfaces:**
- Consumes: Task 1의 `postLike` 델리게이트, `TransactionClient`(`../../outbox/domain/transaction-runner`).
- Produces:
  - `POST_LIKE_REPOSITORY: symbol`
  - `interface PostLikeRepository`:
    - `like(postId: string, userId: string, tx?: TransactionClient): Promise<boolean>` — 신규 생성 true / 이미 있음 false
    - `unlike(postId: string, userId: string, tx?: TransactionClient): Promise<boolean>` — 삭제됨 true / 없었음 false
    - `countByPost(postId: string): Promise<number>`
    - `countByPosts(postIds: string[]): Promise<Map<string, number>>`
    - `likedPostIds(userId: string, postIds: string[]): Promise<Set<string>>`
    - `hasLiked(postId: string, userId: string): Promise<boolean>`

- [ ] **Step 1: 도메인 포트 작성**

Create `src/board/domain/post-like.repository.ts`:

```ts
import { TransactionClient } from '../../outbox/domain/transaction-runner';

export const POST_LIKE_REPOSITORY = Symbol('POST_LIKE_REPOSITORY');

// 게시글 좋아요 저장소. like/unlike는 상태 전이 여부(bool)를 돌려준다 —
// DB 영향 행 수(rowCount) 기반이라, 이벤트 발행을 신규 좋아요일 때만 하도록 게이팅한다.
export interface PostLikeRepository {
  // INSERT ... ON CONFLICT DO NOTHING → 신규 생성이면 true.
  like(postId: string, userId: string, tx?: TransactionClient): Promise<boolean>;
  // DELETE WHERE postId,userId → 실제 삭제됐으면 true.
  unlike(postId: string, userId: string, tx?: TransactionClient): Promise<boolean>;
  // 단건 게시글 좋아요 수(라이브 COUNT).
  countByPost(postId: string): Promise<number>;
  // 목록용 배치: postId → 좋아요 수. 없는 글은 맵에 없음(호출측 0 처리).
  countByPosts(postIds: string[]): Promise<Map<string, number>>;
  // 목록용 배치: 주어진 글들 중 userId가 좋아요한 postId 집합.
  likedPostIds(userId: string, postIds: string[]): Promise<Set<string>>;
  // 상세용: 이 사용자가 이 글을 좋아요했는가.
  hasLiked(postId: string, userId: string): Promise<boolean>;
}
```

- [ ] **Step 2: 실패하는 스펙 작성**

Create `src/board/infrastructure/prisma-post-like.repository.spec.ts`:

```ts
import { PrismaPostLikeRepository } from './prisma-post-like.repository';
import { PrismaService } from '../../prisma/prisma.service';

const POST_ID = 'p1';
const USER_ID = 'u1';

// PrismaService는 거대 생성 타입이라 필요한 postLike 메서드만 mock한다.
function createMockPrisma() {
  return {
    postLike: {
      createMany: jest.fn(),
      deleteMany: jest.fn(),
      count: jest.fn(),
      groupBy: jest.fn(),
      findMany: jest.fn(),
      findUnique: jest.fn(),
    },
  };
}

describe('PrismaPostLikeRepository', () => {
  let prisma: ReturnType<typeof createMockPrisma>;
  let repo: PrismaPostLikeRepository;

  beforeEach(() => {
    prisma = createMockPrisma();
    repo = new PrismaPostLikeRepository(prisma as unknown as PrismaService);
  });

  afterEach(() => {
    jest.clearAllMocks();
  });

  describe('like', () => {
    it('신규 좋아요면 createMany(skipDuplicates)로 넣고 true를 반환한다', async () => {
      prisma.postLike.createMany.mockResolvedValue({ count: 1 });

      const result = await repo.like(POST_ID, USER_ID);

      expect(prisma.postLike.createMany).toHaveBeenCalledWith({
        data: [{ postId: POST_ID, userId: USER_ID }],
        skipDuplicates: true,
      });
      expect(result).toBe(true);
    });

    it('이미 좋아요한 상태면 count 0 → false를 반환한다', async () => {
      prisma.postLike.createMany.mockResolvedValue({ count: 0 });

      const result = await repo.like(POST_ID, USER_ID);

      expect(result).toBe(false);
    });
  });

  describe('unlike', () => {
    it('삭제된 행이 있으면 true를 반환한다', async () => {
      prisma.postLike.deleteMany.mockResolvedValue({ count: 1 });

      const result = await repo.unlike(POST_ID, USER_ID);

      expect(prisma.postLike.deleteMany).toHaveBeenCalledWith({
        where: { postId: POST_ID, userId: USER_ID },
      });
      expect(result).toBe(true);
    });

    it('삭제할 행이 없으면 false를 반환한다', async () => {
      prisma.postLike.deleteMany.mockResolvedValue({ count: 0 });

      const result = await repo.unlike(POST_ID, USER_ID);

      expect(result).toBe(false);
    });
  });

  describe('countByPost', () => {
    it('해당 글의 좋아요 수를 COUNT로 조회한다', async () => {
      prisma.postLike.count.mockResolvedValue(3);

      const result = await repo.countByPost(POST_ID);

      expect(prisma.postLike.count).toHaveBeenCalledWith({
        where: { postId: POST_ID },
      });
      expect(result).toBe(3);
    });
  });

  describe('countByPosts', () => {
    it('빈 배열이면 쿼리 없이 빈 맵', async () => {
      const result = await repo.countByPosts([]);

      expect(prisma.postLike.groupBy).not.toHaveBeenCalled();
      expect(result.size).toBe(0);
    });

    it('groupBy 결과를 postId→count 맵으로 변환한다', async () => {
      prisma.postLike.groupBy.mockResolvedValue([
        { postId: 'p1', _count: { _all: 2 } },
        { postId: 'p2', _count: { _all: 5 } },
      ]);

      const result = await repo.countByPosts(['p1', 'p2', 'p3']);

      expect(result.get('p1')).toBe(2);
      expect(result.get('p2')).toBe(5);
      expect(result.has('p3')).toBe(false);
    });
  });

  describe('likedPostIds', () => {
    it('빈 배열이면 쿼리 없이 빈 셋', async () => {
      const result = await repo.likedPostIds(USER_ID, []);

      expect(prisma.postLike.findMany).not.toHaveBeenCalled();
      expect(result.size).toBe(0);
    });

    it('유저가 좋아요한 postId 집합을 반환한다', async () => {
      prisma.postLike.findMany.mockResolvedValue([{ postId: 'p1' }]);

      const result = await repo.likedPostIds(USER_ID, ['p1', 'p2']);

      expect(prisma.postLike.findMany).toHaveBeenCalledWith({
        where: { userId: USER_ID, postId: { in: ['p1', 'p2'] } },
        select: { postId: true },
      });
      expect(result.has('p1')).toBe(true);
      expect(result.has('p2')).toBe(false);
    });
  });

  describe('hasLiked', () => {
    it('행이 있으면 true', async () => {
      prisma.postLike.findUnique.mockResolvedValue({ id: 'l1' });

      const result = await repo.hasLiked(POST_ID, USER_ID);

      expect(prisma.postLike.findUnique).toHaveBeenCalledWith({
        where: { postId_userId: { postId: POST_ID, userId: USER_ID } },
        select: { id: true },
      });
      expect(result).toBe(true);
    });

    it('행이 없으면 false', async () => {
      prisma.postLike.findUnique.mockResolvedValue(null);

      const result = await repo.hasLiked(POST_ID, USER_ID);

      expect(result).toBe(false);
    });
  });
});
```

- [ ] **Step 3: 스펙 실패 확인**

Run: `npm test -- prisma-post-like.repository`
Expected: FAIL — `Cannot find module './prisma-post-like.repository'`.

- [ ] **Step 4: Prisma 구현 작성**

Create `src/board/infrastructure/prisma-post-like.repository.ts`:

```ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../../prisma/prisma.service';
import { PostLikeRepository } from '../domain/post-like.repository';
import { TransactionClient } from '../../outbox/domain/transaction-runner';

@Injectable()
export class PrismaPostLikeRepository implements PostLikeRepository {
  constructor(private readonly prisma: PrismaService) {}

  async like(
    postId: string,
    userId: string,
    tx?: TransactionClient,
  ): Promise<boolean> {
    const db = tx ?? this.prisma;
    // ON CONFLICT DO NOTHING. 유니크 제약이 동시 insert를 직렬화 → 정확히 하나만 count 1.
    const { count } = await db.postLike.createMany({
      data: [{ postId, userId }],
      skipDuplicates: true,
    });
    return count === 1;
  }

  async unlike(
    postId: string,
    userId: string,
    tx?: TransactionClient,
  ): Promise<boolean> {
    const db = tx ?? this.prisma;
    const { count } = await db.postLike.deleteMany({ where: { postId, userId } });
    return count === 1;
  }

  async countByPost(postId: string): Promise<number> {
    return this.prisma.postLike.count({ where: { postId } });
  }

  async countByPosts(postIds: string[]): Promise<Map<string, number>> {
    if (postIds.length === 0) return new Map();
    const rows = await this.prisma.postLike.groupBy({
      by: ['postId'],
      where: { postId: { in: postIds } },
      _count: { _all: true },
    });
    return new Map(rows.map((r) => [r.postId, r._count._all]));
  }

  async likedPostIds(userId: string, postIds: string[]): Promise<Set<string>> {
    if (postIds.length === 0) return new Set();
    const rows = await this.prisma.postLike.findMany({
      where: { userId, postId: { in: postIds } },
      select: { postId: true },
    });
    return new Set(rows.map((r) => r.postId));
  }

  async hasLiked(postId: string, userId: string): Promise<boolean> {
    const row = await this.prisma.postLike.findUnique({
      where: { postId_userId: { postId, userId } },
      select: { id: true },
    });
    return row !== null;
  }
}
```

- [ ] **Step 5: 스펙 통과 확인**

Run: `npm test -- prisma-post-like.repository`
Expected: PASS (전 케이스 green).

- [ ] **Step 6: Commit**

```bash
git add src/board/domain/post-like.repository.ts src/board/infrastructure/prisma-post-like.repository.ts src/board/infrastructure/prisma-post-like.repository.spec.ts
git commit -m "[M2]feat: PostLikeRepository 포트·Prisma 구현(멱등 like/unlike, 배치 집계)"
```

---

### Task 3: LikeCreated 이벤트 등록 + 알림 매핑

**Files:**
- Modify: `src/events/event-type.enum.ts`
- Modify: `src/notification/domain/notification-type.enum.ts`
- Modify: `src/notification/domain/notification-content.ts`
- Test: `src/notification/domain/notification-content.spec.ts` (케이스 추가)
- Modify: `src/notification/infrastructure/prisma-recipient-resolver.ts`
- Test: `src/notification/infrastructure/prisma-recipient-resolver.spec.ts` (케이스 추가)

**Interfaces:**
- Produces: `EventType.LikeCreated`, `NotificationType.PostLiked`. `buildContent`가 `LikeCreated` → PostLiked 매핑, resolver가 `LikeCreated` → 작성자(본인 제외) 반환.
- Consumes(다음 태스크): 좋아요 outbox 이벤트는 `entityType=EntityType.Post`, `entityId=postId`, `payload={ postId, buildingId }`, `actorId=likerId`.

- [ ] **Step 1: `EventType`에 LikeCreated 추가 + 토픽 매핑**

`src/events/event-type.enum.ts`:
- `enum EventType`에 `MessageSent` 아래 추가: `LikeCreated = 'LikeCreated',`
- `TOPIC_BY_EVENT` 객체에 추가: `[EventType.LikeCreated]: KafkaTopic.BoardEvents,`

(entityType은 기존 `EntityType.Post` 재사용 — enum 추가 불필요.)

- [ ] **Step 2: `NotificationType`에 PostLiked 추가**

`src/notification/domain/notification-type.enum.ts`의 `enum NotificationType`에 추가:

```ts
  PostLiked = 'PostLiked',
```

- [ ] **Step 3: content 스펙에 실패 케이스 추가**

`src/notification/domain/notification-content.spec.ts`의 `describe('buildContent', ...)` 안에 추가:

```ts
  it('LikeCreated → PostLiked, entityId는 postId', () => {
    const c = buildContent(
      event({
        eventType: EventType.LikeCreated,
        entityType: EntityType.Post,
        entityId: 'p7',
        payload: { postId: 'p7', buildingId: 'b7' },
      }),
    );

    expect(c).toEqual({
      type: NotificationType.PostLiked,
      title: '새 좋아요',
      body: '회원님의 글에 좋아요가 눌렸습니다',
      entityType: EntityType.Post,
      entityId: 'p7',
      buildingId: 'b7',
    });
  });
```

- [ ] **Step 4: content 스펙 실패 확인**

Run: `npm test -- notification-content`
Expected: FAIL — `LikeCreated` 케이스가 없어 `buildContent`가 `null` 반환 → `toEqual` 불일치.

- [ ] **Step 5: `buildContent`에 LikeCreated 케이스 추가**

`src/notification/domain/notification-content.ts`의 `switch` 문에서 `case EventType.PostCreated:` 블록 위(또는 아래)에 추가:

```ts
    case EventType.LikeCreated: {
      const p = event.payload as { postId: string; buildingId: string | null };
      return {
        type: NotificationType.PostLiked,
        title: '새 좋아요',
        body: '회원님의 글에 좋아요가 눌렸습니다',
        entityType: EntityType.Post,
        entityId: p.postId,
        buildingId: p.buildingId,
      };
    }
```

- [ ] **Step 6: content 스펙 통과 확인**

Run: `npm test -- notification-content`
Expected: PASS.

- [ ] **Step 7: resolver 스펙에 실패 케이스 추가**

`src/notification/infrastructure/prisma-recipient-resolver.spec.ts`의 `describe` 안에 추가:

```ts
  it('LikeCreated: 글 작성자에게, 단 본인 좋아요면 제외', async () => {
    prisma.post.findFirst.mockResolvedValue({ authorId: 'author1' });
    const base: DomainEvent = {
      eventId: 'e1',
      eventType: EventType.LikeCreated,
      occurredAt: '2026-07-03T00:00:00.000Z',
      actorId: 'liker1',
      entityType: EntityType.Post,
      entityId: 'p1',
      payload: { postId: 'p1', buildingId: 'b1' },
    };

    await expect(resolver.resolve(base)).resolves.toEqual(['author1']);
    await expect(
      resolver.resolve({ ...base, actorId: 'author1' }),
    ).resolves.toEqual([]);
  });
```

- [ ] **Step 8: resolver 스펙 실패 확인**

Run: `npm test -- prisma-recipient-resolver`
Expected: FAIL — `LikeCreated`가 `default: return []`로 빠져 첫 단언 `['author1']` 불일치.

- [ ] **Step 9: resolver에 LikeCreated 케이스 추가**

`src/notification/infrastructure/prisma-recipient-resolver.ts`의 `resolve` `switch`에 추가(기존 `forComment` 재사용 — 작성자 반환·본인 제외 로직이 동일):

```ts
      case EventType.LikeCreated:
        return this.forComment(
          event.payload as { postId: string },
          event.actorId,
        );
```

- [ ] **Step 10: resolver 스펙 통과 확인**

Run: `npm test -- prisma-recipient-resolver`
Expected: PASS.

- [ ] **Step 11: Commit**

```bash
git add src/events/event-type.enum.ts src/notification
git commit -m "[M2]feat: LikeCreated 이벤트·PostLiked 알림 매핑(작성자 알림, 본인 제외)"
```

---

### Task 4: LikePostUseCase

**Files:**
- Create: `src/board/application/like-post.use-case.ts`
- Test: `src/board/application/like-post.use-case.spec.ts`

**Interfaces:**
- Consumes: `POST_REPOSITORY`, `POST_LIKE_REPOSITORY`(Task 2), `MEMBERSHIP_CHECKER`, `TRANSACTION_RUNNER`, `OUTBOX_STORE`. `EventType.LikeCreated`/`EntityType.Post`(Task 3).
- Produces: `LikePostUseCase.execute(input: { userId: string; postId: string }): Promise<{ postId: string; liked: true; likeCount: number }>`.

- [ ] **Step 1: 실패하는 스펙 작성**

Create `src/board/application/like-post.use-case.spec.ts`:

```ts
import { LikePostUseCase } from './like-post.use-case';
import { Post } from '../domain/post.entity';
import { PostCategory } from '../domain/post-category.enum';
import { PostRepository } from '../domain/post.repository';
import { PostLikeRepository } from '../domain/post-like.repository';
import { MembershipChecker } from './membership';
import {
  TransactionRunner,
  TransactionClient,
} from '../../outbox/domain/transaction-runner';
import { OutboxStore } from '../../outbox/domain/outbox-store';
import { EventType, EntityType } from '../../events/event-type.enum';

const POST_ID = 'p1';
const BUILDING_ID = 'b1';
const USER_ID = 'u1';
const AUTHOR_ID = 'author';

const TX = {} as unknown as TransactionClient;

const txRunner: TransactionRunner = { run: (fn) => fn(TX) };

function membershipReturning(value: boolean): MembershipChecker {
  return { isMember: () => Promise.resolve(value) };
}

function postRepoWith(post: Post | null): PostRepository {
  return {
    create: (p) => Promise.resolve(p),
    findById: () => Promise.resolve(post),
    findByBuilding: () => Promise.resolve([]),
    update: (p) => Promise.resolve(p),
    delete: () => Promise.resolve(),
  };
}

// like 전이 결과·count를 제어할 수 있는 fake 좋아요 저장소
function likeRepoWith(opts: {
  newlyLiked: boolean;
  count: number;
}): { likes: PostLikeRepository; getLastTx: () => TransactionClient | undefined } {
  let lastTx: TransactionClient | undefined;
  const likes: PostLikeRepository = {
    like: (_p, _u, tx) => {
      lastTx = tx;
      return Promise.resolve(opts.newlyLiked);
    },
    unlike: () => Promise.resolve(false),
    countByPost: () => Promise.resolve(opts.count),
    countByPosts: () => Promise.resolve(new Map()),
    likedPostIds: () => Promise.resolve(new Set()),
    hasLiked: () => Promise.resolve(false),
  };
  return { likes, getLastTx: () => lastTx };
}

function outboxSpy(added: unknown[]): OutboxStore {
  return {
    add: (e) => {
      added.push(e);
      return Promise.resolve();
    },
    fetchPending: () => Promise.resolve([]),
    markPublished: () => Promise.resolve(),
    markFailed: () => Promise.resolve({ quarantined: false }),
  };
}

const samplePost = Post.reconstitute({
  id: POST_ID,
  buildingId: BUILDING_ID,
  authorId: AUTHOR_ID,
  category: PostCategory.FREE,
  title: '제목',
  content: '본문',
});

describe('LikePostUseCase', () => {
  it('신규 좋아요면 tx로 저장하고 outbox에 LikeCreated를 적재하며 likeCount를 반환한다', async () => {
    const added: unknown[] = [];
    const { likes, getLastTx } = likeRepoWith({ newlyLiked: true, count: 1 });

    const useCase = new LikePostUseCase(
      postRepoWith(samplePost),
      likes,
      membershipReturning(true),
      txRunner,
      outboxSpy(added),
    );

    const result = await useCase.execute({ userId: USER_ID, postId: POST_ID });

    expect(result).toEqual({ postId: POST_ID, liked: true, likeCount: 1 });
    expect(getLastTx()).toBe(TX);
    expect(added).toEqual([
      expect.objectContaining({
        eventType: EventType.LikeCreated,
        entityType: EntityType.Post,
        entityId: POST_ID,
        actorId: USER_ID,
        payload: expect.objectContaining({
          postId: POST_ID,
          buildingId: BUILDING_ID,
        }) as object,
      }),
    ]);
  });

  it('이미 좋아요한 상태(재클릭)면 outbox에 적재하지 않는다(멱등)', async () => {
    const added: unknown[] = [];
    const { likes } = likeRepoWith({ newlyLiked: false, count: 1 });

    const useCase = new LikePostUseCase(
      postRepoWith(samplePost),
      likes,
      membershipReturning(true),
      txRunner,
      outboxSpy(added),
    );

    const result = await useCase.execute({ userId: USER_ID, postId: POST_ID });

    expect(result).toEqual({ postId: POST_ID, liked: true, likeCount: 1 });
    expect(added).toEqual([]);
  });

  it('없는 글이면 BOARD_POST_NOT_FOUND', async () => {
    const { likes } = likeRepoWith({ newlyLiked: true, count: 0 });

    const useCase = new LikePostUseCase(
      postRepoWith(null),
      likes,
      membershipReturning(true),
      txRunner,
      outboxSpy([]),
    );

    await expect(
      useCase.execute({ userId: USER_ID, postId: POST_ID }),
    ).rejects.toMatchObject({ code: 'BOARD_POST_NOT_FOUND' });
  });

  it('멤버가 아니면 BOARD_NOT_BUILDING_MEMBER', async () => {
    const { likes } = likeRepoWith({ newlyLiked: true, count: 0 });

    const useCase = new LikePostUseCase(
      postRepoWith(samplePost),
      likes,
      membershipReturning(false),
      txRunner,
      outboxSpy([]),
    );

    await expect(
      useCase.execute({ userId: USER_ID, postId: POST_ID }),
    ).rejects.toMatchObject({ code: 'BOARD_NOT_BUILDING_MEMBER' });
  });
});
```

- [ ] **Step 2: 스펙 실패 확인**

Run: `npm test -- like-post.use-case`
Expected: FAIL — `Cannot find module './like-post.use-case'`.

- [ ] **Step 3: 유스케이스 구현**

Create `src/board/application/like-post.use-case.ts`:

```ts
import { Inject, Injectable } from '@nestjs/common';
import { randomUUID } from 'node:crypto';
import { POST_REPOSITORY, PostRepository } from '../domain/post.repository';
import {
  POST_LIKE_REPOSITORY,
  PostLikeRepository,
} from '../domain/post-like.repository';
import { MEMBERSHIP_CHECKER, MembershipChecker } from './membership';
import { AppException } from '../../common/errors/app-exception';
import { BoardError } from '../board.errors';
import { EventType, EntityType } from '../../events/event-type.enum';
import {
  TRANSACTION_RUNNER,
  TransactionRunner,
} from '../../outbox/domain/transaction-runner';
import { OUTBOX_STORE, OutboxStore } from '../../outbox/domain/outbox-store';

export interface LikePostInput {
  userId: string;
  postId: string;
}

export interface LikePostResult {
  postId: string;
  liked: true;
  likeCount: number;
}

@Injectable()
export class LikePostUseCase {
  constructor(
    @Inject(POST_REPOSITORY) private readonly posts: PostRepository,
    @Inject(POST_LIKE_REPOSITORY) private readonly likes: PostLikeRepository,
    @Inject(MEMBERSHIP_CHECKER) private readonly membership: MembershipChecker,
    @Inject(TRANSACTION_RUNNER) private readonly txRunner: TransactionRunner,
    @Inject(OUTBOX_STORE) private readonly outbox: OutboxStore,
  ) {}

  async execute(input: LikePostInput): Promise<LikePostResult> {
    const post = await this.posts.findById(input.postId);
    if (!post) throw new AppException(BoardError.POST_NOT_FOUND);
    const ok = await this.membership.isMember(input.userId, post.buildingId);
    if (!ok) throw new AppException(BoardError.NOT_BUILDING_MEMBER);

    // 좋아요 insert + outbox 적재를 한 트랜잭션으로. 신규 전이(newlyLiked)일 때만
    // 이벤트를 발행해 재클릭 스팸을 막는다(전이 판단은 DB rowCount 기반).
    await this.txRunner.run(async (tx) => {
      const newlyLiked = await this.likes.like(input.postId, input.userId, tx);
      if (newlyLiked) {
        await this.outbox.add(
          {
            eventId: randomUUID(),
            eventType: EventType.LikeCreated,
            occurredAt: new Date().toISOString(),
            actorId: input.userId,
            entityType: EntityType.Post,
            entityId: input.postId,
            payload: { postId: input.postId, buildingId: post.buildingId },
          },
          tx,
        );
      }
    });

    // 라이브 COUNT — 커밋 후 최신 수치.
    const likeCount = await this.likes.countByPost(input.postId);
    return { postId: input.postId, liked: true, likeCount };
  }
}
```

- [ ] **Step 4: 스펙 통과 확인**

Run: `npm test -- like-post.use-case`
Expected: PASS (전 케이스 green).

- [ ] **Step 5: Commit**

```bash
git add src/board/application/like-post.use-case.ts src/board/application/like-post.use-case.spec.ts
git commit -m "[M2]feat: LikePostUseCase(멱등 좋아요, 신규 전이만 이벤트 발행)"
```

---

### Task 5: UnlikePostUseCase

**Files:**
- Create: `src/board/application/unlike-post.use-case.ts`
- Test: `src/board/application/unlike-post.use-case.spec.ts`

**Interfaces:**
- Consumes: `POST_REPOSITORY`, `POST_LIKE_REPOSITORY`, `MEMBERSHIP_CHECKER`, `TRANSACTION_RUNNER`.
- Produces: `UnlikePostUseCase.execute(input: { userId: string; postId: string }): Promise<{ postId: string; liked: false; likeCount: number }>`.

- [ ] **Step 1: 실패하는 스펙 작성**

Create `src/board/application/unlike-post.use-case.spec.ts`:

```ts
import { UnlikePostUseCase } from './unlike-post.use-case';
import { Post } from '../domain/post.entity';
import { PostCategory } from '../domain/post-category.enum';
import { PostRepository } from '../domain/post.repository';
import { PostLikeRepository } from '../domain/post-like.repository';
import { MembershipChecker } from './membership';
import {
  TransactionRunner,
  TransactionClient,
} from '../../outbox/domain/transaction-runner';

const POST_ID = 'p1';
const BUILDING_ID = 'b1';
const USER_ID = 'u1';

const TX = {} as unknown as TransactionClient;
const txRunner: TransactionRunner = { run: (fn) => fn(TX) };

function membershipReturning(value: boolean): MembershipChecker {
  return { isMember: () => Promise.resolve(value) };
}

function postRepoWith(post: Post | null): PostRepository {
  return {
    create: (p) => Promise.resolve(p),
    findById: () => Promise.resolve(post),
    findByBuilding: () => Promise.resolve([]),
    update: (p) => Promise.resolve(p),
    delete: () => Promise.resolve(),
  };
}

function likeRepoWith(count: number): {
  likes: PostLikeRepository;
  getLastTx: () => TransactionClient | undefined;
} {
  let lastTx: TransactionClient | undefined;
  const likes: PostLikeRepository = {
    like: () => Promise.resolve(false),
    unlike: (_p, _u, tx) => {
      lastTx = tx;
      return Promise.resolve(true);
    },
    countByPost: () => Promise.resolve(count),
    countByPosts: () => Promise.resolve(new Map()),
    likedPostIds: () => Promise.resolve(new Set()),
    hasLiked: () => Promise.resolve(false),
  };
  return { likes, getLastTx: () => lastTx };
}

const samplePost = Post.reconstitute({
  id: POST_ID,
  buildingId: BUILDING_ID,
  authorId: 'author',
  category: PostCategory.FREE,
  title: '제목',
  content: '본문',
});

describe('UnlikePostUseCase', () => {
  it('멤버가 좋아요를 취소하면 tx로 삭제하고 갱신된 likeCount를 반환한다', async () => {
    const { likes, getLastTx } = likeRepoWith(0);

    const useCase = new UnlikePostUseCase(
      postRepoWith(samplePost),
      likes,
      membershipReturning(true),
      txRunner,
    );

    const result = await useCase.execute({ userId: USER_ID, postId: POST_ID });

    expect(result).toEqual({ postId: POST_ID, liked: false, likeCount: 0 });
    expect(getLastTx()).toBe(TX);
  });

  it('없는 글이면 BOARD_POST_NOT_FOUND', async () => {
    const { likes } = likeRepoWith(0);

    const useCase = new UnlikePostUseCase(
      postRepoWith(null),
      likes,
      membershipReturning(true),
      txRunner,
    );

    await expect(
      useCase.execute({ userId: USER_ID, postId: POST_ID }),
    ).rejects.toMatchObject({ code: 'BOARD_POST_NOT_FOUND' });
  });

  it('멤버가 아니면 BOARD_NOT_BUILDING_MEMBER', async () => {
    const { likes } = likeRepoWith(0);

    const useCase = new UnlikePostUseCase(
      postRepoWith(samplePost),
      likes,
      membershipReturning(false),
      txRunner,
    );

    await expect(
      useCase.execute({ userId: USER_ID, postId: POST_ID }),
    ).rejects.toMatchObject({ code: 'BOARD_NOT_BUILDING_MEMBER' });
  });
});
```

- [ ] **Step 2: 스펙 실패 확인**

Run: `npm test -- unlike-post.use-case`
Expected: FAIL — `Cannot find module './unlike-post.use-case'`.

- [ ] **Step 3: 유스케이스 구현**

Create `src/board/application/unlike-post.use-case.ts`:

```ts
import { Inject, Injectable } from '@nestjs/common';
import { POST_REPOSITORY, PostRepository } from '../domain/post.repository';
import {
  POST_LIKE_REPOSITORY,
  PostLikeRepository,
} from '../domain/post-like.repository';
import { MEMBERSHIP_CHECKER, MembershipChecker } from './membership';
import { AppException } from '../../common/errors/app-exception';
import { BoardError } from '../board.errors';
import {
  TRANSACTION_RUNNER,
  TransactionRunner,
} from '../../outbox/domain/transaction-runner';

export interface UnlikePostInput {
  userId: string;
  postId: string;
}

export interface UnlikePostResult {
  postId: string;
  liked: false;
  likeCount: number;
}

@Injectable()
export class UnlikePostUseCase {
  constructor(
    @Inject(POST_REPOSITORY) private readonly posts: PostRepository,
    @Inject(POST_LIKE_REPOSITORY) private readonly likes: PostLikeRepository,
    @Inject(MEMBERSHIP_CHECKER) private readonly membership: MembershipChecker,
    @Inject(TRANSACTION_RUNNER) private readonly txRunner: TransactionRunner,
  ) {}

  async execute(input: UnlikePostInput): Promise<UnlikePostResult> {
    const post = await this.posts.findById(input.postId);
    if (!post) throw new AppException(BoardError.POST_NOT_FOUND);
    const ok = await this.membership.isMember(input.userId, post.buildingId);
    if (!ok) throw new AppException(BoardError.NOT_BUILDING_MEMBER);

    // 취소는 멱등 물리삭제(없으면 no-op). 이벤트는 발행하지 않는다.
    await this.txRunner.run(async (tx) => {
      await this.likes.unlike(input.postId, input.userId, tx);
    });

    const likeCount = await this.likes.countByPost(input.postId);
    return { postId: input.postId, liked: false, likeCount };
  }
}
```

- [ ] **Step 4: 스펙 통과 확인**

Run: `npm test -- unlike-post.use-case`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/board/application/unlike-post.use-case.ts src/board/application/unlike-post.use-case.spec.ts
git commit -m "[M2]feat: UnlikePostUseCase(멱등 좋아요 취소)"
```

---

### Task 6: GetPost/ListPosts에 좋아요 병합

**Files:**
- Modify: `src/board/application/board-cache.ts` (view 타입 추가)
- Modify: `src/board/application/get-post.use-case.ts`
- Test: `src/board/application/get-post.use-case.spec.ts` (생성자 인자 추가 + 케이스)
- Modify: `src/board/application/list-posts.use-case.ts`
- Test: `src/board/application/list-posts.use-case.spec.ts` (생성자 인자 추가 + 케이스)
- Modify: `src/board/board.module.ts` (GetPost/ListPosts에 `POST_LIKE_REPOSITORY` 주입)

**Interfaces:**
- Consumes: `POST_LIKE_REPOSITORY`(Task 2)의 `countByPost`/`hasLiked`/`countByPosts`/`likedPostIds`.
- Produces:
  - `interface PostSummaryView extends PostSummary { likeCount: number; likedByMe: boolean }`
  - `interface PostDetailView extends PostDetail { likeCount: number; likedByMe: boolean }`
  - `GetPostUseCase.execute(...) : Promise<PostDetailView>`
  - `ListPostsUseCase.execute(...) : Promise<PostSummaryView[]>`

- [ ] **Step 1: board-cache.ts에 view 타입 추가**

`src/board/application/board-cache.ts`의 `PostDetail` 인터페이스 아래에 추가(기존 `PostSummary`/`PostDetail`은 캐시 계약이므로 **변경하지 않음**):

```ts
// API 응답용 확장 타입. 좋아요 정보는 캐시에 넣지 않고 use-case가 라이브로 병합한다.
export interface PostSummaryView extends PostSummary {
  likeCount: number;
  likedByMe: boolean;
}

export interface PostDetailView extends PostDetail {
  likeCount: number;
  likedByMe: boolean;
}
```

- [ ] **Step 2: get-post 스펙 갱신(생성자 인자 + 병합 케이스)**

`src/board/application/get-post.use-case.spec.ts` 수정:

먼저 상단 import에 추가:

```ts
import { PostLikeRepository } from '../domain/post-like.repository';
```

`membershipReturning` 헬퍼 아래에 fake 좋아요 저장소 헬퍼 추가:

```ts
function likeRepoWith(opts: { count: number; liked: boolean }): PostLikeRepository {
  return {
    like: () => Promise.resolve(false),
    unlike: () => Promise.resolve(false),
    countByPost: () => Promise.resolve(opts.count),
    countByPosts: () => Promise.resolve(new Map()),
    likedPostIds: () => Promise.resolve(new Set()),
    hasLiked: () => Promise.resolve(opts.liked),
  };
}
```

기존 4개 `new GetPostUseCase(...)` 호출 모두에 **네 번째 인자로** `likeRepoWith(...)`를 추가한다(멤버십 인자 다음). 예:
- 첫 번째 테스트: `membershipReturning(true), likeRepoWith({ count: 2, liked: true })`
- 나머지(에러 케이스): `membershipReturning(...), likeRepoWith({ count: 0, liked: false })`

첫 번째 테스트('캐시 miss...')에 단언 추가:

```ts
    expect(detail.likeCount).toBe(2);
    expect(detail.likedByMe).toBe(true);
```

그리고 병합 전용 케이스를 `describe` 안에 추가:

```ts
  it('캐시 hit이어도 좋아요 정보(likeCount·likedByMe)를 라이브로 병합한다', async () => {
    const cache = new FakeCache();
    cache.detail = {
      id: POST_ID,
      buildingId: BUILDING_ID,
      category: PostCategory.FREE,
      title: '제목',
      content: '본문',
      authorId: USER_ID,
      comments: [],
    };
    const useCase = new GetPostUseCase(
      postRepoWith(samplePost),
      commentRepo,
      cache,
      membershipReturning(true),
      likeRepoWith({ count: 5, liked: false }),
    );

    const detail = await useCase.execute({ userId: USER_ID, postId: POST_ID });

    expect(detail.likeCount).toBe(5);
    expect(detail.likedByMe).toBe(false);
    expect(cache.setDetailCalls).toBe(0); // 캐시 hit이라 재적재 없음
  });
```

- [ ] **Step 3: get-post 스펙 실패 확인**

Run: `npm test -- get-post.use-case`
Expected: FAIL — 생성자 인자 개수 불일치(타입 에러) 또는 `likeCount`가 `undefined`.

- [ ] **Step 4: get-post 유스케이스 구현**

`src/board/application/get-post.use-case.ts` 전체를 다음으로 교체:

```ts
import { Inject, Injectable } from '@nestjs/common';
import { POST_REPOSITORY, PostRepository } from '../domain/post.repository';
import {
  COMMENT_REPOSITORY,
  CommentRepository,
} from '../domain/comment.repository';
import {
  POST_LIKE_REPOSITORY,
  PostLikeRepository,
} from '../domain/post-like.repository';
import {
  BOARD_CACHE,
  BoardCache,
  PostDetail,
  PostDetailView,
} from './board-cache';
import { MEMBERSHIP_CHECKER, MembershipChecker } from './membership';
import { AppException } from '../../common/errors/app-exception';
import { BoardError } from '../board.errors';

export interface GetPostInput {
  userId: string;
  postId: string;
}

@Injectable()
export class GetPostUseCase {
  constructor(
    @Inject(POST_REPOSITORY) private readonly posts: PostRepository,
    @Inject(COMMENT_REPOSITORY) private readonly comments: CommentRepository,
    @Inject(BOARD_CACHE) private readonly cache: BoardCache,
    @Inject(MEMBERSHIP_CHECKER) private readonly membership: MembershipChecker,
    @Inject(POST_LIKE_REPOSITORY) private readonly likes: PostLikeRepository,
  ) {}

  async execute(input: GetPostInput): Promise<PostDetailView> {
    const detail = await this.loadDetail(input);

    // 좋아요 정보는 캐시에 넣지 않고 매 조회 시 라이브로 병합(§6).
    const likeCount = await this.likes.countByPost(input.postId);
    const likedByMe = await this.likes.hasLiked(input.postId, input.userId);
    return { ...detail, likeCount, likedByMe };
  }

  // 정적 본문(캐시된 또는 새로 구성한 PostDetail)을 반환. 인가 포함.
  private async loadDetail(input: GetPostInput): Promise<PostDetail> {
    const cached = await this.cache.getDetail(input.postId);
    if (cached) {
      await this.authorize(input.userId, cached.buildingId);
      return cached;
    }

    const post = await this.posts.findById(input.postId);
    if (!post) throw new AppException(BoardError.POST_NOT_FOUND);
    await this.authorize(input.userId, post.buildingId);

    const comments = await this.comments.findByPost(input.postId);
    const detail: PostDetail = {
      id: post.id!,
      buildingId: post.buildingId,
      category: post.category,
      title: post.title,
      content: post.content,
      authorId: post.authorId,
      comments: comments.map((c) => ({
        id: c.id!,
        authorId: c.authorId,
        content: c.content,
      })),
    };
    await this.cache.setDetail(input.postId, detail);
    return detail;
  }

  private async authorize(userId: string, buildingId: string): Promise<void> {
    const ok = await this.membership.isMember(userId, buildingId);
    if (!ok) throw new AppException(BoardError.NOT_BUILDING_MEMBER);
  }
}
```

- [ ] **Step 5: get-post 스펙 통과 확인**

Run: `npm test -- get-post.use-case`
Expected: PASS.

- [ ] **Step 6: list-posts 스펙 갱신**

`src/board/application/list-posts.use-case.spec.ts`를 연다. 상단 import에 `import { PostLikeRepository } from '../domain/post-like.repository';` 추가.

fake 좋아요 저장소 헬퍼 추가(파일 내 헬퍼 근처):

```ts
function likeRepoWith(opts: {
  counts: Map<string, number>;
  liked: Set<string>;
}): PostLikeRepository {
  return {
    like: () => Promise.resolve(false),
    unlike: () => Promise.resolve(false),
    countByPost: () => Promise.resolve(0),
    countByPosts: () => Promise.resolve(opts.counts),
    likedPostIds: () => Promise.resolve(opts.liked),
    hasLiked: () => Promise.resolve(false),
  };
}
```

기존 `new ListPostsUseCase(...)` 호출들에 마지막 인자로 좋아요 저장소를 추가한다. 정상 목록 케이스에는 실제 postId에 맞춘 값을 준다(예: 글 id가 `p1`이면 `likeRepoWith({ counts: new Map([['p1', 4]]), liked: new Set(['p1']) })`), 에러 케이스에는 `likeRepoWith({ counts: new Map(), liked: new Set() })`.

정상 목록 케이스에 단언 추가(반환 배열의 해당 항목에 대해):

```ts
    expect(result[0].likeCount).toBe(4);
    expect(result[0].likedByMe).toBe(true);
```

> 참고: 기존 정상 케이스가 목록이 비어있게 설계돼 있다면, 글 하나를 반환하도록 `findByBuilding`/캐시 fake를 조정하고 위 단언을 적용한다.

- [ ] **Step 7: list-posts 스펙 실패 확인**

Run: `npm test -- list-posts.use-case`
Expected: FAIL — 생성자 인자 불일치 또는 `likeCount` undefined.

- [ ] **Step 8: list-posts 유스케이스 구현**

`src/board/application/list-posts.use-case.ts` 전체를 교체:

```ts
import { Inject, Injectable } from '@nestjs/common';
import { POST_REPOSITORY, PostRepository } from '../domain/post.repository';
import {
  POST_LIKE_REPOSITORY,
  PostLikeRepository,
} from '../domain/post-like.repository';
import {
  BOARD_CACHE,
  BoardCache,
  PostSummary,
  PostSummaryView,
} from './board-cache';
import { MEMBERSHIP_CHECKER, MembershipChecker } from './membership';
import { AppException } from '../../common/errors/app-exception';
import { BoardError } from '../board.errors';

export interface ListPostsInput {
  userId: string;
  buildingId: string;
}

@Injectable()
export class ListPostsUseCase {
  constructor(
    @Inject(POST_REPOSITORY) private readonly posts: PostRepository,
    @Inject(BOARD_CACHE) private readonly cache: BoardCache,
    @Inject(MEMBERSHIP_CHECKER) private readonly membership: MembershipChecker,
    @Inject(POST_LIKE_REPOSITORY) private readonly likes: PostLikeRepository,
  ) {}

  async execute(input: ListPostsInput): Promise<PostSummaryView[]> {
    const ok = await this.membership.isMember(input.userId, input.buildingId);
    if (!ok) throw new AppException(BoardError.NOT_BUILDING_MEMBER);

    const summaries = await this.loadSummaries(input.buildingId);

    // 좋아요 정보는 캐시 밖에서 배치로 병합(N+1 회피).
    const postIds = summaries.map((s) => s.id);
    const counts = await this.likes.countByPosts(postIds);
    const liked = await this.likes.likedPostIds(input.userId, postIds);
    return summaries.map((s) => ({
      ...s,
      likeCount: counts.get(s.id) ?? 0,
      likedByMe: liked.has(s.id),
    }));
  }

  private async loadSummaries(buildingId: string): Promise<PostSummary[]> {
    const cached = await this.cache.getList(buildingId);
    if (cached) return cached;

    const posts = await this.posts.findByBuilding(buildingId);
    const summaries: PostSummary[] = posts.map((p) => ({
      id: p.id!,
      category: p.category,
      title: p.title,
      authorId: p.authorId,
    }));
    await this.cache.setList(buildingId, summaries);
    return summaries;
  }
}
```

- [ ] **Step 9: list-posts 스펙 통과 확인**

Run: `npm test -- list-posts.use-case`
Expected: PASS.

- [ ] **Step 10: 모듈에 POST_LIKE_REPOSITORY 배선**

`src/board/board.module.ts` 수정:
- import 추가:
  ```ts
  import { POST_LIKE_REPOSITORY } from './domain/post-like.repository';
  import { PrismaPostLikeRepository } from './infrastructure/prisma-post-like.repository';
  ```
- `providers` 배열에 추가:
  ```ts
  { provide: POST_LIKE_REPOSITORY, useClass: PrismaPostLikeRepository },
  ```

- [ ] **Step 11: 빌드로 배선 검증**

Run: `npm run build`
Expected: 에러 없음(GetPost/ListPosts의 새 의존성이 모듈에서 해소됨).

- [ ] **Step 12: Commit**

```bash
git add src/board/application/board-cache.ts src/board/application/get-post.use-case.ts src/board/application/get-post.use-case.spec.ts src/board/application/list-posts.use-case.ts src/board/application/list-posts.use-case.spec.ts src/board/board.module.ts
git commit -m "[M2]feat: 게시글 상세·목록에 likeCount·likedByMe 라이브 병합"
```

---

### Task 7: 컨트롤러 엔드포인트 + Swagger + 모듈 배선 + README

**Files:**
- Modify: `src/board/board.module.ts` (Like/Unlike 유스케이스 provider 등록)
- Modify: `src/board/interface/board.controller.ts` (POST/DELETE 라우트)
- Modify: `README.md` (board API 표)

**Interfaces:**
- Consumes: `LikePostUseCase`(Task 4), `UnlikePostUseCase`(Task 5).
- Produces:
  - `POST /posts/:postId/likes` → 201 `{ postId, liked: true, likeCount }`
  - `DELETE /posts/:postId/likes` → 200 `{ postId, liked: false, likeCount }`

- [ ] **Step 1: 모듈에 유스케이스 등록**

`src/board/board.module.ts`:
- import 추가:
  ```ts
  import { LikePostUseCase } from './application/like-post.use-case';
  import { UnlikePostUseCase } from './application/unlike-post.use-case';
  ```
- `providers` 배열에 `LikePostUseCase,` `UnlikePostUseCase,` 추가.

- [ ] **Step 2: 컨트롤러에 라우트 추가**

`src/board/interface/board.controller.ts`:
- 생성자 파라미터에 추가:
  ```ts
  private readonly likePost: LikePostUseCase,
  private readonly unlikePost: UnlikePostUseCase,
  ```
- import 추가:
  ```ts
  import { LikePostUseCase } from '../application/like-post.use-case';
  import { UnlikePostUseCase } from '../application/unlike-post.use-case';
  ```
- `HttpCode`가 이미 import돼 있음. 클래스 안 `createCommentHandler` 아래에 두 핸들러 추가:

```ts
  @Post('posts/:postId/likes')
  @ApiParam({ name: 'postId', description: '좋아요할 게시글 ID' })
  @ApiOperation({ summary: '게시글 좋아요(멱등, 건물 멤버 전용)' })
  @ApiResponse({ status: 201, description: '좋아요 반영 후 상태' })
  @ApiResponse({
    status: 403,
    type: ErrorResponseDto,
    description: '건물 멤버 아님',
  })
  @ApiResponse({
    status: 404,
    type: ErrorResponseDto,
    description: '게시글 없음',
  })
  likePostHandler(
    @CurrentUser() user: TokenPayload,
    @Param('postId') postId: string,
  ) {
    return this.likePost.execute({ userId: user.sub, postId });
  }

  @Delete('posts/:postId/likes')
  @ApiParam({ name: 'postId', description: '좋아요를 취소할 게시글 ID' })
  @ApiOperation({ summary: '게시글 좋아요 취소(멱등, 건물 멤버 전용)' })
  @ApiResponse({ status: 200, description: '좋아요 취소 후 상태' })
  @ApiResponse({
    status: 403,
    type: ErrorResponseDto,
    description: '건물 멤버 아님',
  })
  @ApiResponse({
    status: 404,
    type: ErrorResponseDto,
    description: '게시글 없음',
  })
  unlikePostHandler(
    @CurrentUser() user: TokenPayload,
    @Param('postId') postId: string,
  ) {
    return this.unlikePost.execute({ userId: user.sub, postId });
  }
```

> `DELETE`는 본문(likeCount)을 돌려주므로 기존 `deletePostHandler`의 `@HttpCode(204)`와 달리 기본 200을 쓴다(HttpCode 데코레이터 붙이지 않음).

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 에러 없음.

- [ ] **Step 4: 전체 테스트 검증**

Run: `npm test`
Expected: 전 스위트 PASS(기존 + 신규).

- [ ] **Step 5: README API 표 갱신**

`README.md`의 board(게시판) API 표에 두 행 추가:

```markdown
| `POST` | `/posts/:postId/likes` | 게시글 좋아요(멱등) | 인증 · 건물 멤버 |
| `DELETE` | `/posts/:postId/likes` | 게시글 좋아요 취소(멱등) | 인증 · 건물 멤버 |
```

그리고 게시글 상세·목록 조회 행 설명에 "응답에 `likeCount`·`likedByMe` 포함"을 덧붙인다. (표의 정확한 컬럼 구성은 기존 board 표 형식을 따른다.)

- [ ] **Step 6: Commit**

```bash
git add src/board/board.module.ts src/board/interface/board.controller.ts README.md
git commit -m "[M2]feat: 좋아요/취소 엔드포인트 추가(POST·DELETE /posts/:postId/likes)"
```

---

## Self-Review

**1. Spec coverage:**
- §3 데이터 모델 → Task 1 ✓
- §4 도메인 포트 + Prisma 구현 → Task 2 ✓
- §5 Like/Unlike 유스케이스 → Task 4·5 ✓
- §6 응답 병합·캐시 전략(캐시 미저장, 라이브 병합) → Task 6 ✓
- §7 동시성(원자적 rowCount·라이브 COUNT·전이 게이팅) → Task 2(리포지토리)·Task 4(전이 게이팅) ✓
- §8 이벤트/알림 배선 → Task 3 ✓
- §9 엔드포인트·Swagger → Task 7 ✓
- §10 모듈 배선 → Task 6(리포지토리)·Task 7(유스케이스) ✓
- §11 테스트 → 각 태스크 스펙 ✓
- §12 알려진 한계(재-좋아요 재알림) → 코드 동작(전이 게이팅)으로 재클릭 스팸만 차단, 취소→재좋아요 재알림은 의도된 한계로 수용 ✓

**2. Placeholder scan:** TBD/TODO 없음. 모든 코드 스텝에 실제 코드 포함.

**3. Type consistency:**
- `PostLikeRepository` 메서드 시그니처가 포트(Task 2)·fake(Task 4·5·6)·Prisma 구현(Task 2)에서 일치.
- `like/unlike`의 반환 `Promise<boolean>` 일관.
- `PostDetailView`/`PostSummaryView`가 board-cache.ts에 정의(Task 6 Step 1)되고 get/list에서 참조.
- 이벤트 페이로드 `{ postId, buildingId }`가 발행(Task 4)·소비(Task 3 content/resolver)에서 일치.
