# M7: k6 API 부하테스트 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 성격이 다른 대표 엔드포인트 4개에 k6 부하 시나리오(smoke/load 프로파일 + thresholds)를 만들고, 멱등 시드로 데이터를 준비해 성능 baseline(p95·RPS·에러율)을 측정·기록한다.

**Architecture:** `load/` 폴더에 k6(JS) 스크립트를 둔다. `config.js`가 env로 프로파일·threshold를 분기하고, `lib/auth.js`가 `setup()`에서 시드 유저로 로그인해 토큰·buildingId를 확보한다. 시나리오 4개(read/create/login/rate-limit)가 각자 thresholds로 합격 판정한다. `prisma/seed-load.ts`가 부하용 OWNER·건물·글을 멱등 생성한다.

**Tech Stack:** k6(외부 도구, JS ES module), Prisma(seed, ts-node), 기존 NestJS API.

> **설계 스펙:** [docs/superpowers/specs/2026-06-16-m7-load-test-design.md](../specs/2026-06-16-m7-load-test-design.md)

---

## 사전 준비

- 작업 브랜치: `feat/m7-load-test` (이미 `dev`에서 분기됨).
- **k6 설치 필요**(코드 외부): macOS `brew install k6`. 미설치면 시나리오 실행 스텝은 "k6 미설치 시 건너뛰고 스크립트 정합성만 검증"으로 처리.
- 인프라: `docker compose up -d`(PG·Redis·Kafka). 앱: `node dist/main.js`.
- 컨벤션: 커밋 `[M7]{타입}: {한글}` + 트레일러 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. k6 스크립트는 jest 대상 아님(ESLint 대상에서도 `load/`는 제외 — Task 1에서 처리).

## 확인된 기존 사실 (구현 근거)

- 비밀번호 해시: `BcryptPasswordHasher`, **bcrypt rounds=10**. 시드도 `bcrypt.hash(pw, 10)`으로 저장해야 로그인 가능.
- 로그인 응답: `POST /auth/login` → `{ "accessToken": string }`.
- 게시글 목록(멤버 전용): `GET /buildings/:buildingId/posts` (Bearer 필요).
- 글 작성(멤버 전용): `POST /buildings/:buildingId/posts` body `{ category, title, content }`.
- `signup`은 role을 받지 않음 → seed에서 `role:'OWNER'` 직접 지정. OWNER는 자기 건물의 멤버로 인정됨(`PrismaMembershipChecker`).
- 모델: `User{ id,email@unique,passwordHash,name,role,... }`, `Building{ id,ownerId,name,address,... }`, `Post{ buildingId,authorId,category,title,content,... }`.
- rate limit env: `RATE_LIMIT_USER_MAX`, `RATE_LIMIT_IP_MAX`, `RATE_LIMIT_WINDOW_SEC`(M6).

## 파일 구조 (생성/수정 맵)

```
load/
  README.md                실행법·프로파일·threshold·결과 기록 표
  config.js                BASE_URL·PROFILE(smoke/load)·VUS·threshold 상수 + env 오버라이드
  lib/auth.js              login()·setupContext() — 토큰·buildingId 획득
  scenarios/
    read-posts.js          GET 게시글 목록
    create-post.js         POST 글작성
    login.js               POST 로그인
    rate-limit.js          429 경계 검증
prisma/seed-load.ts        부하용 OWNER·건물·글 멱등 생성
수정:
  package.json             load:seed / load:smoke / load:read / load:create / load:login / load:ratelimit
  eslint.config / .eslintignore  load/ 제외(k6 전역 import는 ESLint 대상 아님)
  README.md                §6 마일스톤 M7 + load/ 안내·k6 설치
  docs/study/마일스톤-학습-노트.md   부하테스트 학습 포인트(별도 커밋)
```

---

## Task 1: 시드 스크립트 (prisma/seed-load.ts)

**Files:**
- Create: `prisma/seed-load.ts`
- Modify: `package.json` (scripts에 `load:seed`)

> 멱등(여러 번 실행 가능): 같은 email/건물명이 있으면 재사용. 부하 대상 데이터(OWNER·건물·글 5개) 보장.

- [ ] **Step 1: seed 스크립트 작성**

`prisma/seed-load.ts`:

```ts
// 부하테스트용 고정 데이터 시드(멱등). 앱과 동일한 bcrypt rounds=10으로 해시해야 로그인 가능.
import { PrismaClient } from '@prisma/client';
import * as bcrypt from 'bcrypt';

const prisma = new PrismaClient();

// 부하 스크립트(lib/auth.js)와 공유하는 고정 자격증명.
const LOAD_EMAIL = 'load-owner@example.com';
const LOAD_PASSWORD = 'load-test-1234';
const BUILDING_NAME = 'LoadTest Tower';
const SEED_POST_COUNT = 5;

async function main(): Promise<void> {
  const passwordHash = await bcrypt.hash(LOAD_PASSWORD, 10);

  // OWNER 유저 upsert(이미 있으면 role·해시 보정).
  const owner = await prisma.user.upsert({
    where: { email: LOAD_EMAIL },
    update: { role: 'OWNER', passwordHash, deletedAt: null },
    create: {
      email: LOAD_EMAIL,
      name: 'Load Owner',
      passwordHash,
      role: 'OWNER',
    },
  });

  // 건물: 이름으로 조회해 없으면 생성(고정 1개).
  let building = await prisma.building.findFirst({
    where: { name: BUILDING_NAME, ownerId: owner.id },
  });
  building ??= await prisma.building.create({
    data: { ownerId: owner.id, name: BUILDING_NAME, address: 'Seoul' },
  });

  // 읽기 시나리오용 글 채우기(부족분만 생성).
  const existing = await prisma.post.count({
    where: { buildingId: building.id },
  });
  for (let i = existing; i < SEED_POST_COUNT; i++) {
    await prisma.post.create({
      data: {
        buildingId: building.id,
        authorId: owner.id,
        category: 'FREE',
        title: `부하테스트 글 ${i + 1}`,
        content: '시드 데이터',
      },
    });
  }

  console.log(
    JSON.stringify(
      {
        email: LOAD_EMAIL,
        password: LOAD_PASSWORD,
        buildingId: building.id,
        posts: Math.max(existing, SEED_POST_COUNT),
      },
      null,
      2,
    ),
  );
}

main()
  .catch((err) => {
    console.error(err);
    process.exit(1);
  })
  .finally(() => void prisma.$disconnect());
```

- [ ] **Step 2: package.json 스크립트**

`scripts`에 추가:

```jsonc
"load:seed": "ts-node prisma/seed-load.ts"
```

- [ ] **Step 3: 실행 검증** (docker compose 기동 상태)

Run: `npm run load:seed`
Expected: JSON 출력(email·password·buildingId·posts). 재실행해도 에러 없이 같은 buildingId(멱등).

- [ ] **Step 4: 커밋**

```bash
git add prisma/seed-load.ts package.json
git commit -m "[M7]feat: 부하테스트용 멱등 시드(OWNER·건물·글)"
```

---

## Task 2: k6 설정 + 인증 헬퍼 (config.js, lib/auth.js)

**Files:**
- Create: `load/config.js`
- Create: `load/lib/auth.js`
- Create: `load/README.md` (골격; Task 6에서 결과 표 채움)

> k6는 자체 JS 런타임(ESM, `import { http } from 'k6/http'`). Node가 아니므로 jest/eslint 대상이 아니다.

- [ ] **Step 1: config.js**

`load/config.js`:

```js
// 부하 프로파일·threshold·공통 설정. env로 오버라이드한다.
// 예: BASE_URL=http://localhost:3000 PROFILE=load k6 run load/scenarios/read-posts.js
export const BASE_URL = __ENV.BASE_URL || 'http://localhost:3000';

// 시드(prisma/seed-load.ts)와 공유하는 고정 자격증명.
export const SEED = {
  email: __ENV.LOAD_EMAIL || 'load-owner@example.com',
  password: __ENV.LOAD_PASSWORD || 'load-test-1234',
};

// PROFILE=smoke(정상성) | load(baseline). VUS·DURATION으로 미세 조정.
const PROFILE = __ENV.PROFILE || 'smoke';

const PROFILES = {
  smoke: { vus: Number(__ENV.VUS) || 1, duration: __ENV.DURATION || '30s' },
  load: {
    stages: [
      { duration: '30s', target: Number(__ENV.VUS) || 20 },
      { duration: '1m', target: Number(__ENV.VUS) || 20 },
      { duration: '10s', target: 0 },
    ],
  },
};

export function profileOptions() {
  return PROFILES[PROFILE] || PROFILES.smoke;
}

// 엔드포인트 성격별 threshold(설계 §3).
export const THRESHOLDS = {
  read: { http_req_duration: ['p95<300'], http_req_failed: ['rate<0.01'] },
  write: { http_req_duration: ['p95<800'], http_req_failed: ['rate<0.01'] },
  login: { http_req_duration: ['p95<1000'], http_req_failed: ['rate<0.01'] },
};
```

- [ ] **Step 2: lib/auth.js**

`load/lib/auth.js`:

```js
import http from 'k6/http';
import { check } from 'k6';
import { BASE_URL, SEED } from '../config.js';

// 시드 OWNER로 로그인해 accessToken을 얻는다(시나리오 setup에서 1회 호출).
export function login() {
  const res = http.post(
    `${BASE_URL}/auth/login`,
    JSON.stringify({ email: SEED.email, password: SEED.password }),
    { headers: { 'Content-Type': 'application/json' } },
  );
  check(res, { 'login 200/201': (r) => r.status === 200 || r.status === 201 });
  return res.json('accessToken');
}

// 로그인 후 내 건물 목록에서 첫 건물 id를 얻는다(시드가 만든 건물).
export function firstBuildingId(token) {
  const res = http.get(`${BASE_URL}/buildings`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  check(res, { 'buildings 200': (r) => r.status === 200 });
  const list = res.json();
  return Array.isArray(list) && list.length > 0 ? list[0].id : null;
}

export function authHeaders(token) {
  return {
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
  };
}
```

> 주: `GET /buildings`는 OWNER 전용(M1). 시드 유저가 OWNER이므로 자기 건물 목록을 받는다. 응답이 `{id,...}[]` 배열임을 가정(컨트롤러 확인).

- [ ] **Step 3: README 골격**

`load/README.md`:

```markdown
# 부하테스트 (k6)

성격이 다른 핵심 엔드포인트의 성능 baseline(p95·RPS·에러율)을 측정한다.

## 사전 준비
1. k6 설치: `brew install k6` (또는 https://k6.io/docs/get-started/installation/)
2. 인프라: `docker compose up -d`
3. 앱: `npm run build && node dist/main.js` (글작성 부하 시 `npm run start:worker:outbox`도 함께)
4. 시드: `npm run load:seed`

## 실행
| 명령 | 시나리오 |
|---|---|
| `npm run load:smoke` | 전체 smoke(정상성) |
| `npm run load:read` | GET 게시글 목록 |
| `npm run load:create` | POST 글작성 |
| `npm run load:login` | POST 로그인 |
| `npm run load:ratelimit` | 429 경계 |

프로파일/규모: `PROFILE=load VUS=20 k6 run load/scenarios/read-posts.js`

## rate limit 주의
부하가 rate limit에 걸리므로, 측정 시 한도를 크게 띄운다:
`RATE_LIMIT_USER_MAX=100000 RATE_LIMIT_IP_MAX=100000 node dist/main.js`
rate-limit 시나리오는 반대로 낮은 한도로 띄워 429를 검증한다.

## 결과 기록
| 일자 | 시나리오 | 프로파일 | p95(ms) | RPS | 에러율 | 비고 |
|---|---|---|---|---|---|---|
| _(측정 후 기록)_ | | | | | | |
```

- [ ] **Step 4: 커밋**

```bash
git add load/config.js load/lib/auth.js load/README.md
git commit -m "[M7]feat: k6 설정·인증 헬퍼·부하테스트 README"
```

---

## Task 3: 읽기·쓰기 시나리오 (read-posts.js, create-post.js)

**Files:**
- Create: `load/scenarios/read-posts.js`
- Create: `load/scenarios/create-post.js`

- [ ] **Step 1: read-posts.js**

`load/scenarios/read-posts.js`:

```js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { BASE_URL, THRESHOLDS, profileOptions } from '../config.js';
import { login, firstBuildingId, authHeaders } from '../lib/auth.js';

// 읽기·캐시 경로: GET 게시글 목록. Redis read-through 캐시 효과를 본다.
export const options = { ...profileOptions(), thresholds: THRESHOLDS.read };

export function setup() {
  const token = login();
  const buildingId = firstBuildingId(token);
  return { token, buildingId };
}

export default function (data) {
  const res = http.get(
    `${BASE_URL}/buildings/${data.buildingId}/posts`,
    authHeaders(data.token),
  );
  check(res, { 'list 200': (r) => r.status === 200 });
  sleep(1);
}
```

- [ ] **Step 2: create-post.js**

`load/scenarios/create-post.js`:

```js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { BASE_URL, THRESHOLDS, profileOptions } from '../config.js';
import { login, firstBuildingId, authHeaders } from '../lib/auth.js';

// 쓰기·DB+Outbox 경로: POST 글작성. 트랜잭션(글+outbox 한 커밋) 비용을 본다.
export const options = { ...profileOptions(), thresholds: THRESHOLDS.write };

export function setup() {
  const token = login();
  const buildingId = firstBuildingId(token);
  return { token, buildingId };
}

export default function (data) {
  const payload = JSON.stringify({
    category: 'FREE',
    title: `부하 글 ${Date.now()}`,
    content: 'load test',
  });
  const res = http.post(
    `${BASE_URL}/buildings/${data.buildingId}/posts`,
    payload,
    authHeaders(data.token),
  );
  check(res, { 'create 201': (r) => r.status === 201 });
  sleep(1);
}
```

- [ ] **Step 3: 정합성 확인** (k6 설치 시)

Run: `BASE_URL=http://localhost:3000 PROFILE=smoke RATE_LIMIT_USER_MAX=100000 RATE_LIMIT_IP_MAX=100000 k6 run load/scenarios/read-posts.js`
(앱·시드 준비 상태. 한도 env는 앱 기동 시 적용해야 하므로, 앱을 그 env로 띄운 뒤 실행.)
Expected: checks 통과(`list 200` 100%), threshold 통과.
> k6 미설치면 이 스텝은 건너뛰고 스크립트 import/문법만 육안 확인.

- [ ] **Step 4: 커밋**

```bash
git add load/scenarios/read-posts.js load/scenarios/create-post.js
git commit -m "[M7]feat: 읽기(캐시)·쓰기(DB+Outbox) 부하 시나리오"
```

---

## Task 4: 인증·rate limit 시나리오 (login.js, rate-limit.js)

**Files:**
- Create: `load/scenarios/login.js`
- Create: `load/scenarios/rate-limit.js`

- [ ] **Step 1: login.js**

`load/scenarios/login.js`:

```js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { BASE_URL, SEED, THRESHOLDS, profileOptions } from '../config.js';

// 인증 경로: POST 로그인. bcrypt 검증(CPU 바운드) 응답시간을 본다.
export const options = { ...profileOptions(), thresholds: THRESHOLDS.login };

export default function () {
  const res = http.post(
    `${BASE_URL}/auth/login`,
    JSON.stringify({ email: SEED.email, password: SEED.password }),
    { headers: { 'Content-Type': 'application/json' } },
  );
  check(res, { 'login ok': (r) => r.status === 200 || r.status === 201 });
  sleep(1);
}
```

- [ ] **Step 2: rate-limit.js**

`load/scenarios/rate-limit.js`:

```js
import http from 'k6/http';
import { check } from 'k6';
import { Counter } from 'k6/metrics';
import { BASE_URL, SEED } from '../config.js';

// rate limit 경계 검증: 낮은 한도로 띄운 앱에 빠르게 반복 → 429가 나오는지.
// (앱을 RATE_LIMIT_IP_MAX=10 등 낮은 값으로 기동한 상태에서 실행)
// 부하 baseline이 아니라 "한도가 정확히 작동하는가"를 본다.
const got429 = new Counter('rate_limited_429');

export const options = {
  vus: 1,
  iterations: 20, // ipMax보다 충분히 많게
  // 429는 정상 동작이므로 http_req_failed threshold를 두지 않는다.
};

export default function () {
  const res = http.post(
    `${BASE_URL}/auth/login`,
    JSON.stringify({ email: SEED.email, password: SEED.password }),
    { headers: { 'Content-Type': 'application/json' } },
  );
  if (res.status === 429) {
    got429.add(1);
    check(res, {
      '429 body code': (r) => r.json('code') === 'RATE_LIMIT_EXCEEDED',
      '429 Retry-After 존재': (r) => r.headers['Retry-After'] !== undefined,
    });
  }
}

// 한도 초과가 최소 1회 관측돼야 한다.
export function handleSummary(data) {
  const count = data.metrics.rate_limited_429
    ? data.metrics.rate_limited_429.values.count
    : 0;
  console.log(`429 관측 횟수: ${count}`);
  return {};
}
```

- [ ] **Step 3: 정합성 확인** (k6 설치 시)

Run(앱을 낮은 한도로): `RATE_LIMIT_IP_MAX=10 RATE_LIMIT_WINDOW_SEC=60 node dist/main.js` 띄운 뒤
`BASE_URL=http://localhost:3000 k6 run load/scenarios/rate-limit.js`
Expected: 로그에 `429 관측 횟수: N`(N≥1), 429 응답의 `code`·`Retry-After` check 통과.

- [ ] **Step 4: 커밋**

```bash
git add load/scenarios/login.js load/scenarios/rate-limit.js
git commit -m "[M7]feat: 인증(bcrypt)·rate limit 경계 부하 시나리오"
```

---

## Task 5: package.json 스크립트 + ESLint 제외

**Files:**
- Modify: `package.json`
- Modify: ESLint 설정(`eslint.config.mjs`/`.js` 또는 `.eslintignore`)

- [ ] **Step 1: load 스크립트 추가**

`package.json` `scripts`에 추가(k6는 외부 바이너리):

```jsonc
"load:smoke": "k6 run load/scenarios/read-posts.js",
"load:read": "k6 run load/scenarios/read-posts.js",
"load:create": "k6 run load/scenarios/create-post.js",
"load:login": "k6 run load/scenarios/login.js",
"load:ratelimit": "k6 run load/scenarios/rate-limit.js"
```

- [ ] **Step 2: ESLint에서 load/ 제외**

ESLint 설정 파일을 확인(`ls eslint.config.* .eslintignore 2>/dev/null`)하고, k6 스크립트가 lint 대상에 들지 않게 한다.
- flat config(`eslint.config.mjs`)면 `ignores: ['load/**', ...]`에 추가.
- `.eslintignore` 방식이면 `load/` 한 줄 추가.

이유: k6 스크립트는 `k6/http` 등 k6 런타임 전용 import를 쓰므로 Node/TS ESLint 규칙(`import/no-unresolved`, `__ENV` 전역 등)에 위배된다.

- [ ] **Step 3: lint 회귀 확인**

Run: `npx eslint src` (또는 프로젝트 lint 명령)
Expected: `load/`가 대상에서 빠져 0 에러 유지. (`npx eslint .` 시에도 load/ 무시 확인.)

- [ ] **Step 4: 커밋**

```bash
git add package.json eslint.config.mjs .eslintignore
git commit -m "[M7]chore: load:* npm 스크립트 + ESLint에서 load/ 제외"
```

---

## Task 6: 측정 실행 + README/학습 노트

**Files:**
- Modify: `load/README.md` (결과 표)
- Modify: `README.md` (§6 마일스톤)
- Modify: `docs/study/마일스톤-학습-노트.md` (별도 커밋)

> **k6 미설치 환경이면:** 실측 스텝(Step 1)은 "k6 설치 후 사용자가 직접 실행"으로 남기고, README 결과 표는 비워둔 채(_(측정 후 기록)_) 나머지 문서화만 진행한다. 이 경우 보고에 명시한다.

- [ ] **Step 1: baseline 측정** (k6 설치 + 앱·시드 준비)

순서:
```bash
docker compose up -d
npm run build
RATE_LIMIT_USER_MAX=100000 RATE_LIMIT_IP_MAX=100000 node dist/main.js &   # 부하용 한도 상향
npm run start:worker:outbox &                                            # 글작성 적체 관찰
npm run load:seed
PROFILE=load npm run load:read
PROFILE=load npm run load:create
PROFILE=load npm run load:login
```
각 실행의 k6 요약에서 **p95·RPS·http_req_failed**를 기록. rate-limit은 낮은 한도로 별도 1회.

- [ ] **Step 2: threshold 동작 검증(메타)**

`config.js`의 `THRESHOLDS.read.http_req_duration`을 일시로 `['p95<1']`로 낮춰 실행 → k6가 **threshold 실패로 exit 1** 하는지 확인 후 원복. (합격 판정이 실제로 작동함을 증명.)

- [ ] **Step 3: load/README 결과 표 채우기**

측정한 p95·RPS·에러율을 `load/README.md` 결과 표에 기록(최소 read/create/login 각 1행).

- [ ] **Step 4: 루트 README §6 갱신**

마일스톤 표에 행 추가(Outbox 행 아래):
```
| **M7** ✅ | k6 API 부하테스트(성격별 대표 4개 + thresholds) | 성능 baseline·p95·부하 도구 |
```
그리고 §8 실행 방법 근처에 한 줄: "부하테스트는 `load/`(k6) 참고 — `npm run load:seed` 후 `npm run load:read` 등."

- [ ] **Step 5: 커밋(README들)**

```bash
git add load/README.md README.md
git commit -m "[M7]docs: 부하테스트 baseline 측정 결과 + README 마일스톤 M7"
```

- [ ] **Step 6: 학습 노트 갱신(별도 커밋)**

`docs/study/마일스톤-학습-노트.md`:
- §0 표에 행 추가: `| **M7** | k6 API 부하테스트 | p95/p99·RPS·thresholds, smoke vs load, "측정이 측정을 방해"(rate limit) |`
- 새 절(또는 §9 테스트 전략 확장)에 부하테스트 포인트:
  - **왜 평균이 아니라 p95/p99인가** — 평균은 꼬리 지연을 숨긴다. SLO는 보통 백분위로 잡는다.
  - **smoke vs load vs stress/spike** 차이(우리는 앞 둘만).
  - **"측정 대상이 측정을 방해"** — rate limit이 부하를 막는 딜레마와 env 한도 조정으로 푼 방법.
  - **로컬 부하의 한계** — 절대 성능이 아니라 상대 비교·회귀 감지용.
- "스스로 점검"에 추가:
  - `- [ ] 응답시간 SLO를 평균이 아니라 p95/p99로 잡는 이유는?`
  - `- [ ] 부하테스트가 rate limit에 걸릴 때, 가드를 끄지 않고 한도만 올린 이유는?`
  - `- [ ] smoke/load/stress/spike는 각각 무엇을 보려는 부하인가?`

```bash
git add docs/study/마일스톤-학습-노트.md
git commit -m "[M7]docs: 학습 노트에 부하테스트(p95·thresholds·측정 딜레마) 추가"
```

- [ ] **Step 7: 최종 점검**

Run: `npx jest && npm run build && npx eslint src`
Expected: 기존 전체 테스트 통과·빌드·lint 0(load/는 비대상). k6 스크립트는 별도(외부 실행).

---

## 완료 기준 체크리스트

- [ ] `prisma/seed-load.ts`가 멱등하게 OWNER·건물·글을 만든다(로그인 가능한 해시).
- [ ] 시나리오 4개(read/create/login/rate-limit)가 thresholds로 합격 판정.
- [ ] smoke/load 프로파일을 env로 전환 가능.
- [ ] rate-limit 시나리오가 429+code+Retry-After를 검증.
- [ ] README(load/ + 루트 §6) + 학습 노트 갱신(노트 별도 커밋).
- [ ] 기존 jest/build/eslint 무회귀(load/는 lint 비대상).
- [ ] (k6 설치 시) baseline 1회 측정 기록 + threshold 실패 동작 확인.

---

## 실행 핸드오프

이 계획은 **superpowers:subagent-driven-development**로 task 단위 실행을 권장한다. **주의:** k6는 외부 바이너리라 미설치 시 실측 스텝은 사용자 몫으로 남기고 스크립트·문서·무회귀를 완성한다. 또는 **superpowers:executing-plans**로 인라인 실행.
